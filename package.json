{
  "name": "Tabs Pin",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "dev": "web-ext run",
    "build": "node scripts/build-extension.js",
    "package": "web-ext build --overwrite-dest",
    "start": "web-ext run",
    "lint": "web-ext lint"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "web-ext": "^10.1.0"
  }
}
